{
  "whisper": {
    "model": "base",
    "description": "Model size: tiny (small), base (recommended), small, medium, large"
  },
  "audio": {
    "sample_rate": 16000,
    "record_duration": 5,
    "description": "Sample rate in Hz, duration in seconds"
  },
  "matching": {
    "confidence_threshold": 80,
    "fuzzy_match_algorithm": "token_set_ratio",
    "description": "Confidence threshold (0-100) for keyword matching"
  },
  "server": {
    "host": "127.0.0.1",
    "port": 5000,
    "debug": false
  },
  "database": {
    "supplies_file": "medical_supplies_expanded.json"
  },
  "output": {
    "results_file": "test_results.json",
    "save_on_exit": true
  }
}
